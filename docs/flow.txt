resourcePageHandler

- extract iri
- extract language
- determine endpoint (from cookie)
- create "parser" [=querySettings]
    - endpoint
    - timeout
    - list of prefixes
    - list of endpoints
- new resource
    - IRI
    - shortIRI
    - TemplateName
    - TemplatePath
    - Data [parser.TemplateData]
- r.resolveTemplate (
    preprocessor, typeprio, prefixes
)
- r.ProcessTemplate(path, iri, language)

    - read template
    - extract SPARQL queries
    - substitute ??

    - execute queries in parallel, return QueryResults
        - preprocessor.executeQueriesParallel
        - loop for all queries
            - executeQueryWithContext (ctx, query, resolveLabels, language, endpoint)
        - return query results array

    - create data object = iri & results

- GIN render template (r.name, r.data) 
